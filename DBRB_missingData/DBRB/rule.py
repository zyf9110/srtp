# =============================================================================
# Base structure
# =============================================================================
# 置信规则


class Rule:
    def __init__(self, A, D):
        # 初始化
        self.theta = 1  # 规则权重
        self.condition = [0]*len(A)  # 规则前件
        self.beta = [0] * len(D)  # 规则后件

